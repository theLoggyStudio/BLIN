export const DEFAULT_ADMIN_EMAIL = "admin@blin.local"

/** Nombre max de suggestions affichées dans les champs à autocomplétion (relations entité). */
export const RELATION_SUGGESTIONS_LIMIT = 10

/** Taille de page par défaut des listes paginées (hors tableaux). */
export const LIST_PAGE_SIZE = 10

/** Options visibles par page dans les menus Select volumineux. */
export const SELECT_OPTIONS_PAGE_SIZE = 50

/** Seuil au-delà duquel le Select active recherche + pagination interne. */
export const SELECT_OPTIONS_PAGINATE_THRESHOLD = 50

/** Tâches chargées max pour les rappels (évite un dda_list complet). */
export const REMINDER_TASKS_PAGE_SIZE = 200